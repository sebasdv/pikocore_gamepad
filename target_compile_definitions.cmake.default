target_compile_definitions(${PROJECT_NAME} PRIVATE
	PICO_XOSC_STARTUP_DELAY_MULTIPLIER=16
	WS2812_ENABLED=1
	MIDI_IN_ENABLED=1
	MIDI_RESET_EVERY_BEAT=16
	MIDI_CLOCK_MULTIPLIER=2 # reset every 1/8th note
	MIDI_NOTE_KEY=0

	# DEBUG_PWM 1
	# DEBUG_CALIBRATE_PO 1
	# DEBUG_KNOB 1
	# DEBUG_BUTTONS 1
	# DEBUG_SEQUENCER 1
	# DEBUG_SAVE 1
	# DEBUG_CLOCK 1
	# DEBUG_BPM 1
)